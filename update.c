#include<stdio.h>
int main()
{
        int n;
        int v ;
        scanf("%d", &n);
        scanf("%d", &v);
        int a[n];
        for(int i=0;i<n;i++)
        {
            scanf("%d ",&a[i]); 
        }
         for(int i=n-1;i>=0;i--)
         
    return 0;
}